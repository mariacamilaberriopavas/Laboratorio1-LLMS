Pregunta 0383
La función implementada coincide exactamente con el enunciado y con la función definida en el generador de casos de uso del compañero.
Firma de la función (answer-0383.py, línea 1):
def entrenar_pronostico_lasso(df, target_col, alpha_lasso):
La implementación realiza exactamente lo solicitado:

Línea 3: copia el DataFrame con df.copy()
Línea 4: crea la columna ventas_ayer usando df[target_col].shift(1)
Línea 5: elimina valores nulos con df.dropna()
Líneas 7-8: separa variables X e y
Líneas 10-11: entrena un modelo Lasso(alpha=alpha_lasso) y llama a .fit(X, y)
Líneas 13-14: calcula el coeficiente R² con r2_score(y, y_pred)
Línea 15: retorna (modelo, r2) como tupla

El error reportado por el validador fue:
entrenar_pronostico_lasso() missing 3 required positional arguments: 'df', 'target_col', and 'alpha_lasso'
Este error no proviene de mi implementación, sino del generador de casos de uso del compañero. Específicamente, el error indica que en algún punto del proceso de validación automática se intentó invocar la función sin los argumentos requeridos.
Sin embargo, en el generador de casos de uso del compañero la función sí se invoca correctamente con los tres argumentos:

Línea 41 del generador: modelo, r2 = entrenar_pronostico_lasso(df, target_col, alpha_lasso)
Línea 55 del generador (comprobar_caso_de_uso): modelo, r2 = entrenar_pronostico_lasso(input_data["df"], input_data["target_col"], input_data["alpha_lasso"])
La causa más probable es que el validador intentó llamar a entrenar_pronostico_lasso de forma aislada, sin pasar los argumentos, posiblemente por un problema en cómo el generador del compañero fue importado o ejecutado en el entorno de validación automática. Esto es un fallo en el generador de casos de uso de la Fase 1, no en la solución de la Fase 2.
La firma, la lógica y el retorno de mi función son correctos y coinciden exactamente con lo definido en el enunciado y en el generador proporcionado.
